# Frozen Ocean Benchmark Run — 2026-08-10

This branch triggers the reproducible professional-ATP smoke test on 20 frozen ocean instances with L*=10.

Each professional prover invocation has a hard 300-second wall-clock ceiling. The workflow preserves generated instances, raw proof outputs, environment/version information, and summary tables as an artifact.

The earlier Prover9 parallel-build race is avoided by building Prover9 serially. L*=20 and L*=100 remain available in the generator but are deferred until this L*=10 compatibility run is clean.
