# Frozen Ocean Benchmark Run — 2026-08-10

This branch exists to trigger the reproducible professional-ATP benchmark workflow for the frozen L*=20 and L*=100 ocean instances.

Each professional prover invocation has a hard 300-second wall-clock ceiling. The workflow preserves generated instances, raw proof outputs, environment/version information, and summary tables as an artifact.

Retry note: Prover9 is built serially after its top-level parallel build exposed a race in the first workflow attempt.
