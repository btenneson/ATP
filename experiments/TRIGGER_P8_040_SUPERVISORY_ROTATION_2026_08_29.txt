Launch one preregistered-style treatment run on prcom: operational R3 supervisory control + certificate-coherent I4 FUTUREBANK + half-turn rotation of actual strategy knobs; seed 2301; 300000 expansions; no brute fallback.

Preflight history: runs 33254877325, 33254961107, and 33255008712 all stopped before set.mm download and before the scientific search. They respectively caught a Python module-path issue, a legacy selftest interaction, and a mathematically incorrect endpoint identification in the first circle normalization. The treatment uses the antipodal cosine embedding u=(1-cos(theta))/2, so the half-turn theta->theta+pi induces u->1-u and swaps distinct legal endpoints correctly. No preflight spent the 300000-expansion scientific budget.

Scientific run 33255061991 reached ROTATED after stale=6000 and emitted a 36-step candidate at total expansion 6071. Its in-process verifier accepted the candidate, but the separate workflow verifier incorrectly tried to verify the emitted proof fragment as a standalone Metamath database and therefore failed on an environment label. This rerun leaves the search architecture, target, seed, budget, and rotation trigger unchanged. The tightened verifier substitutes only the emitted candidate proof body into the prcom proof slot of the frozen set.mm environment and verifies that resulting environment. The summary now uses the verifier process return code rather than substring matching. This is a verification/instrumentation repair, not a search-policy change.
